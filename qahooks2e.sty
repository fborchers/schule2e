\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{../../qahooks2e}[2023/10/10 v0.0.0 für die Kompatibilität mit der exam documentclass]


\RequirePackage{amsmath}
\RequirePackage{amsthm}



%%
% Die Umgebung 'questions' wird nicht definiert, sie wird nur bei den Klassenarbeiten (exam documentclass) verwendet, nicht in den Skripten. 


%% 
% Markup von newtheorem definieren:
\newtheoremstyle{examquestion}% Name des Stils
	{}% Abstand oberhalb
	{}% Abstand unterhalb
	{}% Markup: Schriftart/-größe, z.B. \itshape
	{}%	Einrückung
	{\itshape}% Markup der Überschrift
	{}% Zeichensetzung nach der Überschrift, z.B. :
	{1em}% Abstand (mindestens ein Leerzeichen, eine \newline möglich)
	{\thmname{#1}\thmnumber{ #2}}% Kontrukt der Überschrift
% Alle folgenden Aufrufe von newtheorem werden diesen Markup verwenden.
\theoremstyle{examquestion}

%%
% Umgebung 'question':
\newtheorem{question}{Aufgabe}


%%
% Den Zähler nach jeder 'section' zurücksetzen. Dieser Code
% kann auskommentiert werden, damit die Aufgaben durch alle Abschnitte
% hindurch weitergezählt werden. 
%\makeatletter
%\@addtoreset{question}{section}
%\makeatother


